import { BaseDecoratorCompose } from '../../common';
import { COMMON_PURCHASE_DECORATORS } from './swagger.decorators';

export const CreatePurchaseDocs = BaseDecoratorCompose(
  COMMON_PURCHASE_DECORATORS,
);
